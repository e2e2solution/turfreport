{
  "name": "turf-match-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start:server": "npm start --prefix server",
    "build": "npm run build --prefix client",
    "build:owner-apk": "npm run build:owner --prefix client && npm run cap:sync --prefix client",
    "start:prod": "cross-env NODE_ENV=production npm start --prefix server"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
